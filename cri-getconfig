# -*- bash -*-

getconfig () {
        perl -e '
                use Osstest;
                readglobalconfig();
die "'$1' caps err" if "'$1'" =~ m/A-Z/;
                print $c{"'$1'"} or die $!;
        '
}
